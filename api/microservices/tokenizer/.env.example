# Copy to .env and adjust. All values are optional (defaults shown).

LOG_LEVEL=INFO

# Registry key selected when a /tokenize request omits "tokenizer". Must name a
# registered backend (currently: cl100k_base) or the service fails to start.
TOKENIZER_DEFAULT=cl100k_base
