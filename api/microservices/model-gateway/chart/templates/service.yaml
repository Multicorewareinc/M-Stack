apiVersion: v1
kind: Service
metadata:
  name: {{ include "model-gateway.fullname" . }}
  labels: {{- include "model-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      # By name, so a changed containerPort cannot leave the Service
      # pointing at a port nothing listens on.
      targetPort: http
      protocol: TCP
  selector: {{- include "model-gateway.selectorLabels" . | nindent 4 }}
